# Format: struct <Name><<T1, size_t N2, ...>> from "<header>"
#   typename T  = type template parameter
#   size_t N    = non-type template parameter (value)

struct StaticVector<T, size_t N> from "utils/StaticVector.hpp"
